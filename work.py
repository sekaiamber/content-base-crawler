# This is the work file
import sys
# You can import each class of work namespace
from work.webpage import WebPage


# config is the config instance
print(config)
# driver is the webdriver instance
print(driver)
# timer is the Timer instance, see work/timer.py
print(timer)
